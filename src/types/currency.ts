export enum Locales {
	frCA = "fr-CA",
	enCA = "en-CA",
	enUS = "en-US"
}

export enum Currency {
	CAD = "CAD",
	USD = "USD"
}

export enum CurrencyStyle {
	Decimal = "decimal",
	Currency = "currency",
	Percent = "percent",
	Unit = "unit"
}
